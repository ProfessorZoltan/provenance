import fs from 'node:fs';
import path from 'node:path';
const root=path.resolve('public/art');
fs.copyFileSync('docs/GAME_DESIGN_v0.1.md',path.join(root,'GAME_DESIGN_v0.1.md'));
const groups=['characters','deep-sites','waypoints','support'];
const entries=groups.flatMap(g=>JSON.parse(fs.readFileSync(path.join(root,'manifests',g+'.json'),'utf8')));
for (const entry of entries) entry.era = entry.era == null ? null : String(entry.era);
const ids=new Set(); const files=new Set();
for(const e of entries){
 if(ids.has(e.id))throw Error(`Duplicate art id: ${e.id}`);ids.add(e.id);
 for(const [role,file] of Object.entries(e.files)){
  if(typeof file!=='string'||file.includes('..')||path.isAbsolute(file))throw Error(`Invalid path ${e.id}/${role}`);
  const full=path.join(root,file);if(!fs.existsSync(full))throw Error(`Missing ${file}`);
  const s=fs.readFileSync(full,'utf8');
  if(!s.includes('<svg')||!s.includes('viewBox=')||!s.includes('crispEdges'))throw Error(`Invalid pixel SVG ${file}`);
  if(/<script|<foreignObject|(?:href|src)=["']https?:|var\(--/i.test(s))throw Error(`Asset is not standalone: ${file}`);
  files.add(file);
 }
}
const catalog={version:1,style:'Original FF1-inspired pixel SVG',source:'docs/GAME_DESIGN_v0.1.md',entryCount:entries.length,fileCount:files.size,entries};
fs.writeFileSync(path.join(root,'catalog.json'),JSON.stringify(catalog,null,2)+'\n');
// Small bundled metadata only; image content stays in public/art and loads on demand.
fs.mkdirSync('src/art',{recursive:true});fs.writeFileSync('src/art/catalog.json',JSON.stringify(entries.map(({id,kind,name,era,site,files})=>({id,kind,name,era,site,files})),null,2)+'\n');
const esc=s=>String(s??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('"','&quot;');
const cards=entries.map(e=>{const file=e.files.hub??e.files.scene??e.files.map??e.files.portrait??e.files.idle??e.files.battle??e.files.icon??Object.values(e.files)[0]; const wide=['deep-site','waypoint','ripple-variant','world-map','ending'].includes(e.kind)||/dungeon/.test(e.id);return `<button class="card ${wide?'wide':''}" data-id="${esc(e.id)}" data-kind="${esc(e.kind)}" data-era="${esc(e.era||'shared')}" data-search="${esc((e.name+' '+e.id+' '+e.description).toLowerCase())}"><div class="preview"><img loading="lazy" src="${esc(file)}" alt="${esc(e.name)}"></div><div class="meta"><small>${esc(e.kind)} / ${esc(e.era||'ALL ERAS')}</small><strong>${esc(e.name)}</strong><code>${esc(e.id)}</code></div></button>`;}).join('');
const html=`<!doctype html><html lang="en"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover"><meta name="theme-color" content="#101b2a"><title>Provenance · Art Library</title><style>
:root{color-scheme:dark;font:15px/1.5 system-ui,sans-serif;background:#101b2a;color:#e8e8da}*{box-sizing:border-box}body{margin:0}header{padding:36px max(24px,5vw) 24px;border-bottom:1px solid #405367;background:#172638}header small{color:#8ccac7;letter-spacing:3px}h1{font-size:36px;letter-spacing:-1px;margin:8px 0}p{max-width:850px;color:#b3c2cc}a{color:#b9ddd1}nav{display:flex;gap:12px;flex-wrap:wrap;position:sticky;top:0;z-index:2;background:#101b2af5;padding:18px max(24px,5vw);border-bottom:1px solid #405367}input,select,button{font:inherit}input,select{color:inherit;background:#243647;border:1px solid #557078;padding:10px}input{flex:1;min-width:170px}#count{align-self:center;font-size:13px;color:#a6bbb9}.grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(250px,1fr));gap:18px;padding:24px max(24px,5vw)}.card{padding:0;background:#192b3d;border:1px solid #3b5467;text-align:left;color:inherit;cursor:pointer;overflow:hidden}.card:hover,.card:focus-visible{outline:2px solid #dfbf7c;outline-offset:2px}.preview{height:190px;display:grid;place-items:center;background:repeating-conic-gradient(#1c2d3b 0% 25%,#203442 0% 50%) 50%/16px 16px;overflow:hidden}.preview img{image-rendering:pixelated;max-height:160px;max-width:95%;width:auto;min-height:80px}.wide .preview img{width:100%;min-height:0;max-height:none;max-width:none}.meta{padding:14px}.meta small{font-size:10px;color:#92c4bc;text-transform:uppercase;letter-spacing:1px}.meta strong,.meta code{display:block}.meta strong{margin:5px 0}.meta code{font-size:11px;color:#a5b2bd;overflow-wrap:anywhere}[hidden]{display:none!important}dialog{width:min(980px,95vw);max-height:94vh;background:#142638;color:#eceddd;border:2px solid #a2b4c5;padding:24px}dialog::backdrop{background:#030b13cb}.detail-art{height:360px;display:grid;place-items:center;background:#0d1b2d;position:relative;overflow:hidden}.detail-art img{image-rendering:pixelated;max-width:100%;max-height:100%;height:100%;object-fit:contain}.detail-art img.layer{position:absolute;inset:0;width:100%;height:100%}.detail-art:has(.layer){aspect-ratio:16/9;height:auto}.detail-actions{display:flex;gap:8px;flex-wrap:wrap;margin:16px 0}.detail-actions button,#close{background:#304f63;color:#f1e4be;border:1px solid #859faf;padding:7px 12px;cursor:pointer}.detail-actions button[aria-pressed=true]{background:#8a7445;color:white}#close{float:right}code{font-family:monospace}#files{overflow-wrap:anywhere;font-size:13px}footer{padding:24px 5vw;color:#9fadb9} @media(max-width:700px){header{padding:22px 16px}h1{font-size:26px}nav{padding:12px 16px;gap:8px}input,select{padding:8px}.grid{grid-template-columns:repeat(auto-fill,minmax(140px,1fr));gap:10px;padding:14px 16px}.preview{height:130px}.preview img{max-height:110px;min-height:56px}.meta{padding:10px}.meta strong{font-size:14px;margin:3px 0}.detail-art{height:200px}dialog{padding:16px;width:96vw}footer{padding:20px 16px}}
</style><header><small>PROVENANCE / ART ARCHIVE</small><h1>One world. Four centuries.</h1><p>Original pixel artwork for the full design guide: characters, places, changing timelines and combat. Select an asset to inspect poses, scene layers and exact filenames. This library prepares artwork; it does not add new quests or unlock unfinished game content.</p><p>${entries.length} catalog entries · ${files.size} standalone SVG files · <a href="ART_GUIDE.md">Read the art-to-design guide</a> · <a href="catalog.json">Asset manifest</a></p></header><nav><input id="search" aria-label="Search art" placeholder="Search characters, places, roles…"><select id="kind" aria-label="Asset category"><option value="">All categories</option>${[...new Set(entries.map(e=>e.kind))].map(k=>`<option>${esc(k)}</option>`).join('')}</select><select id="era" aria-label="Era"><option value="">All eras</option><option>2031</option><option>2064</option><option>2148</option><option>2312</option><option value="shared">Shared</option></select><span id="count"></span></nav><main class="grid">${cards}</main><dialog id="detail"><button id="close" aria-label="Close detail">Close ×</button><h2 id="name"></h2><div class="detail-art" id="art"></div><div class="detail-actions" id="roles"></div><p id="description"></p><p id="source"></p><div id="files"></div></dialog><footer>Original game assets. No Final Fantasy sprites, licensed character art or remote dependencies. Nearest-neighbor scaling recommended.</footer><script>
const entries=${JSON.stringify(entries).replaceAll('<','\\u003c')};
const byId=new Map(entries.map(e=>[e.id,e]));const cards=[...document.querySelectorAll('.card')];
function filter(){const q=document.querySelector('#search').value.toLowerCase(),k=document.querySelector('#kind').value,era=document.querySelector('#era').value;let n=0;for(const c of cards){c.hidden=!(c.dataset.search.includes(q)&&(!k||c.dataset.kind===k)&&(!era||c.dataset.era===era));if(!c.hidden)n++;}document.querySelector('#count').textContent=n+' entries';}
for(const id of ['search','kind','era'])document.getElementById(id).addEventListener('input',filter);filter();
const dialog=document.querySelector('#detail');document.querySelector('#close').onclick=()=>dialog.close();
for(const card of cards)card.onclick=()=>{const e=byId.get(card.dataset.id);document.querySelector('#name').textContent=e.name;document.querySelector('#description').textContent=e.description;document.querySelector('#source').textContent='Design guide: '+e.guideSection+(e.condition?' · State: '+e.condition:'')+(e.proposed?' · Visual interpretation / proposed asset':'');const roles=document.querySelector('#roles'),art=document.querySelector('#art'),links=document.querySelector('#files');roles.replaceChildren();links.replaceChildren();const show=(role)=>{art.replaceChildren();const im=document.createElement('img');im.src=e.files[role];im.alt=e.name+' '+role;art.append(im);for(const b of roles.children)b.setAttribute('aria-pressed',String(b.textContent===role));};for(const [role,file] of Object.entries(e.files)){const b=document.createElement('button');b.textContent=role;b.onclick=()=>show(role);roles.append(b);const row=document.createElement('div');const a=document.createElement('a');a.href=file;a.textContent=role+' → '+file;a.target='_blank';row.append(a);links.append(row);}if(e.files.sky&&e.files.distant&&e.files.midground&&e.files.foreground){const b=document.createElement('button');b.textContent='Compose layers';b.onclick=()=>{art.replaceChildren();for(const role of ['sky','distant','midground','foreground']){const im=document.createElement('img');im.src=e.files[role];im.alt=role;im.className='layer';art.append(im);}for(const v of roles.children)v.setAttribute('aria-pressed',String(v===b));};roles.append(b);}show(e.files.hub?'hub':e.files.portrait?'portrait':Object.keys(e.files)[0]);dialog.showModal();};
</script></html>`;
fs.writeFileSync(path.join(root,'index.html'),html);
const md=['# Provenance — Art-to-design guide','', 'Source: [Game Design Doc v0.1](GAME_DESIGN_v0.1.md), supplied September 18, 2026. The user’s later FF1-inspired pixel-art direction supersedes the original smooth-vector styling. These are original assets, not copies of Final Fantasy artwork.','',`The library contains **${entries.length} catalog entries** and **${files.size} standalone SVG files**. Open [the visual catalog](index.html) to inspect every asset, pose and layer. Machine-readable inventory: [catalog.json](catalog.json).`,'', '## Art direction and implementation contract','', '- Pixel grid: scenes 320 × 180; character, portrait and icon dimensions are declared in each SVG. Scale by whole-number factors where practical; use `image-rendering: pixelated`.','- Palettes retain the guide’s era identity: warm founding, navy/gold handover, moss/rust quiet, cyan future. Future scenes retain white/cyan architecture against dark blue command panels for legibility.','- Party sprites face left for heroes on the right; enemy sprites face right. Party Echo variants retain left-facing source art; mirror them when used as enemies. SVG files are static pose assets; animate by swapping poses or sheets. Do not interpolate or blur pixel edges.','- Scene files are standalone. Where present, `sky`, `distant`, `midground`, `foreground` compose in that order. `hub` is the complete scene; `battle` is a separate scene composed to leave space for horizontal combat. Never put the full hub beneath all its layers.','- Stable IDs and file roles are in the manifest. Never infer story logic from a filename; map canonical game flags to a variant explicitly. `proposed: true` records inferred art choices, not additions to the narrative canon.','- Most scenes and characters are prepared assets for future development. This does not implement the guide’s unreleased locations, party recruitment, combat abilities or endings.','', '## Coverage and decisions','', '- All eight named party members receive battle poses, dialogue portraits and map tokens. Other-era Echo assets are visual interpretations; the guide does not specify complete biographies or outfits in each era.','- All five Deep Sites × four eras receive hub and battle art, plus scene layers and map icons.','- All twenty explicitly named Waypoints receive art. The guide says “6–8” Waypoints per era but names five per era; no additional places are invented to fill that discrepancy.','- Ripple states, the stacked Meridian dungeon, enemy families, boss forms, named slice NPCs, world-map art, ending illustrations and common HUD pictograms support likely follow-on work.','- Geography, exact appearances, unspecified enemy loadouts and ending compositions are art proposals. Narrative conditions remain those in the original guide.','- Settlements and faction hubs do not gain random encounters merely because artwork exists. Battle art is supplied where relevant to Deep Sites, ruins or wilds.','- These are static game-ready assets and pose sets, not a complete animated cinematic or modular animation engine. Existing CSS supplies ambient motion; reduced-motion must retain readable static states.','', '## Using this library in Claude Code or another editor','', '1. Read this guide and `docs/GAME_DESIGN_v0.1.md` before adding content.','2. Open `public/art/index.html` or `/art/` on the deployed site; search the catalog by name, era or role. `npm run art:export` writes `art-directory.html`, the same page with every SVG inlined, for reading offline or on a phone.','3. Use `public/art/catalog.json` as the complete inventory. All `files` values are relative to `public/art/`. `src/art/catalog.json` is the bundled runtime subset.','4. Use `artAssetUrl(id, role)` and `librarySvg(id, role)` from `src/art/library.ts` to render by stable ID. Do not copy pixel data into game logic.','5. Add gameplay content separately in the existing JSON schemas. An art entry alone is not an unlock or encounter.','6. Edit generator source under `scripts/art-*.mjs`, then run `npm run art:build`. This regenerates SVGs, manifests, this inventory and the visual catalog. Do not hand-edit generated files unless you also update their generator.','7. Run `npm run art:check`, `npm run build`, and `npm test`. In this Windows sandbox use `-- --configLoader runner` for Vite/Vitest if their default config bundler cannot inspect a parent directory.','', '## Runtime integration','', 'The current Kell slice uses the library through the existing rig/background wrappers. Existing character IDs, dialogue IDs and game logic stay compatible. Named NPC portraits replace stand-in party portraits. Background variants are selected explicitly from timeline flags. See `src/art/library.ts`, `src/art/rigs.ts`, `src/art/pixel-backgrounds.ts`, and `src/ui/screens/dialogue.ts`.','', '## Complete design cross-reference',''];
md.splice(md.indexOf('## Complete design cross-reference'), 0,
  '## Copyable integration examples', '',
  '```ts',
  "import { librarySvg, artAssetUrl } from './art/library';",
  "const hero = librarySvg('mara', 'idle', '0 0 32 40');",
  "const portrait = librarySvg('mara', 'portrait', '0 0 32 32', 'portrait-svg');",
  "const battleBackground = artAssetUrl('2064_halden', 'battle');",
  "const echo = librarySvg('mara_echo_2148', 'act', '0 0 32 40');",
  '```', '',
  'A new location can point its existing `background.layers[].art` field to catalog IDs and roles:', '',
  '```json',
  '{ "layers": [',
  '  { "id": "sky", "drift": 0.1, "art": "2064_halden:sky" },',
  '  { "id": "distant", "drift": 0.3, "art": "2064_halden:distant" },',
  '  { "id": "midground", "drift": 0.6, "art": "2064_halden:midground" },',
  '  { "id": "foreground", "drift": 1.0, "art": "2064_halden:foreground" }',
  '], "particles": "glint" }',
  '```', '',
  'For automatic composed battle backgrounds, map a new gameplay location ID to its catalog scene ID in `sceneAssetId`. The catalog is intentionally separate from gameplay IDs. File palettes are explicit and do not inherit CSS variables; edit generator palettes to change an outfit. Future 2312 alterations can combine prepared ripple scenes with a chosen era palette, but arbitrary mixed-timeline rendering remains future engine work.', ''
);
for(const kind of [...new Set(entries.map(e=>e.kind))]){md.push(`### ${kind}`,'','| Asset | Era | Design-guide reference / interpretation | Files |','| --- | --- | --- | --- |');for(const e of entries.filter(e=>e.kind===kind)){const text=(e.guideSection+' — '+e.description+(e.condition?' Condition: '+e.condition:'')).replaceAll('|','/').replaceAll('\n',' ');md.push(`| **${e.name}**<br>\`${e.id}\` | ${e.era??'Shared'} | ${text}${e.proposed?' **Proposed visual interpretation.**':''} | ${Object.entries(e.files).map(([role,f])=>`[${role}](${f})`).join(' · ')} |`);}md.push('');}
md.push('## Known limits','','- No bitmap/raster exports are required; SVGs remain editable and portable.','- World maps are art compositions with proposed coordinates, not authoritative collision or navigation data.','- New NPC archetypes are explicitly proposals. Do not assign them story roles without a game-design decision.','- Pose art is provided for presentation; the battle engine still controls action timing, damage, turn order and targeting.','- No original Final Fantasy game assets were used. The influence is low-resolution pixel composition and the horizontal battle convention.','');
fs.writeFileSync(path.join(root,'ART_GUIDE.md'),md.join('\n'));
fs.mkdirSync('docs',{recursive:true});fs.writeFileSync('docs/ART_GUIDE.md',md.join('\n').replaceAll('](GAME_DESIGN_v0.1.md)','](GAME_DESIGN_v0.1.md)').replace(/\]\((?!https?:|GAME_DESIGN)([^)]+)\)/g,'](../public/art/$1)'));
console.log(`Catalog validated: ${entries.length} entries, ${files.size} SVGs.`);
